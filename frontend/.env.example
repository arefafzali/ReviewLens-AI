# Frontend local environment
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_REVIEWLENS_WORKSPACE_ID=
NEXT_PUBLIC_REVIEWLENS_PRODUCT_ID=
# Optional: ingestion request timeout in milliseconds.
# 0 means no client timeout (recommended for long-running extraction).
NEXT_PUBLIC_INGESTION_TIMEOUT_MS=0
